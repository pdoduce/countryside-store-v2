
import { cookies } from 'next/headers'
import { type Database } from '@/types/supabase' // Optional: if you’ve generated types from Supabase

export const createSupabaseServerClient = () => {
  
}
